type Query {
  hello: String
}

type Mutation {
  Register(
    email: String!
    username: String!
    password: String!
    profile_picture: String
    id: String
  ): String!

  Login(email: String!, password: String!): String!
}
