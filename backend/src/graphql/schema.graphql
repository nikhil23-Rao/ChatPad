type Query {
  GetUserPassword(email: String!): String
}

type Mutation {
  Register(
    email: String!
    username: String!
    password: String
    profile_picture: String
    oauth: Boolean
    id: String
  ): String!

  Login(email: String!, password: String): String!
}
