import React from "react";
import { StyleSheet } from "react-native";

interface IProps {}

const ChatsScreen = ({}: IProps) => {
  return <React.Fragment></React.Fragment>;
};

const ChatsScreenStyles = StyleSheet.create({});

export default ChatsScreen;
